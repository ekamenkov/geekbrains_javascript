1. Вычисление происходит справа налево. alert(a), alert(b) выдают последние вычисленные значения
2. var a = 2;
var x = 1 + (a *= 2);

х=1+(2х2)=5
3. 
var a = +prompt( 'Введите a' ),
    b = +prompt( 'Введите b' ),

else if ( a >= 0 && b >= 0 ) {
    alert( a - b );
}
else if ( a < 0 && b < 0) {
    alert( a * b );
}
else {
    alert( a + b );
}
alert( result );
4-6 сделать пока не успел.